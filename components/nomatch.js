import React from "react"

const NoMatch = () => (
  <div className="error">
    <h1>This page doesn't exist.</h1>
    <p>Return to homepage and follow the instructions.</p>
  </div>
)

export default NoMatch